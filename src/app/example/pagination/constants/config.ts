import { IConfig } from "../../../../../projects/terminatable/src/public-api";

export const CONFIG: IConfig = {
  uniqueField: 'id',
  strip: true,
  hover: true,
  pagination: {
    rows: 5,
  },
};
